{
  "name": "Utopia Durgotsav",
  "version": "1.0.0",
  "private": true,
  "description": "Durgotsav site",
  "dependencies": {
    "framer-motion": "^13.0.0",
    "react": "^18.2.0",
    "react-dom": "^18.2.0",
    "react-intersection-observer": "^11.0.0",
    "react-scripts": "5.0.1"
  },
  "scripts": {
    "start": "react-scripts start",
    "build": "BUILD_PATH='./target' react-scripts build && cp ./package.json ./target && mkdir -p ./target/public && cp ./public/* ./target/public ",
    "test": "react-scripts test",
    "eject": "react-scripts eject"
  },
  "browserslist": {
    "production": [
      ">0.2%",
      "not dead",
      "not op_mini all"
    ],
    "development": [
      "last 1 chrome version",
      "last 1 firefox version",
      "last 1 safari version"
    ]
  }
}
